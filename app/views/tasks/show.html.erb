<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<button type="button" class="btn btn-primary"><%= link_to "Edit task", edit_path %></button>
